# Kopyalayın: cp .env.example .env  ardından aşağıdaki değerleri doldurun.

# ── Google API Keys ──
# Google Maps (harita / konum servisleri)
GOOGLE_MAPS_API_KEY=
# Google AI / Gemini API key (AI tavsiyeleri için)
GOOGLE_API_KEY=
GEMINI_API_KEY=

# ── PostgreSQL ──
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
# Docker içinde: postgres:5432  |  Yerelde: localhost:5432 (otomatik dönüştürülür)
DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@postgres:5432/DBNAME

# ── MCP / Veritabanı (Türkçe değişken adları – isteğe bağlı) ──
# Bu değerler doluysa DATABASE_URL yoksa otomatik oluşturulur (MCP sunucusu vb. için)
KULLANICI_ADI=
VERITABANI_ADI=
SIFRE=

# ── Redis ──
# Docker içinde: redis:6379  |  Yerelde: localhost:6379 (otomatik dönüştürülür)
REDIS_URL=redis://redis:6379/0

# ── Telegram ──
# @BotFather ile oluşturduğunuz bot token
TELEGRAM_BOT_TOKEN=
